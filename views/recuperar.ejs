<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registrar Contraseña</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Oswald:wght@200..700&family=PT+Serif:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center vh-100"
  style="background-image: url('/images/login-bg.jpg');
         background-size: 100vw auto;
         background-repeat: no-repeat;">
  <div class="d-flex flex-column align-items-center w-25 rounded-5 border-0 py-4"
       style="background: rgba(255, 255, 255, 0.8)">
    <img class="w-25 mt-3" src="/images/logo.png" alt="logo">
    <div class="mt-3"><strong>Introduce tu matrícula</strong></div>
    <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-danger"><%= error %></div>
      <% } %>
      
      <% if (typeof mensaje !== 'undefined' && mensaje) { %>
        <div class="alert alert-success"><%= mensaje %></div>
      <% } %>
      
    <form class="d-flex flex-column align-items-center my-3 w-100" action="/usuario/olvido-contrasena" method="POST">
      <input class="rounded-pill border-0 w-75 py-2" 
        style="background-image:url('/images/person.svg');
          background-repeat:no-repeat;
          background-position: 1rem center; 
          padding-left: 2.5rem; font-size: 0.8rem;"
        type="text" placeholder="Matrícula" name="matricula" required>
      <input class="rounded-pill btn btn-outline-dark mt-3" style="font-size: 0.7rem;" type="submit" value="Enviar enlace">
    </form>

    <a href="/usuario/iniciar-sesion" class="text-decoration-none link-dark" style="font-size: 0.7rem;">Volver al inicio de sesión</a>
  </div>
</body>
</html>
